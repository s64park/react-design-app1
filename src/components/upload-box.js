import React from 'react';
import {Button} from 'react-bootstrap';

const UploadBox = (props) => {
    return (
        <Button><i className="fa fa-upload" aria-hidden="true"></i>Upload</Button>
    );
};

export default UploadBox;
